echo "Running ${(C)THING}/${(C)DO}"
echo "Select what type of Site you're adding:"
. ${MY_THINGS}/${THING}/_functions.zsh

######################################

read USER_INPUT

case "${USER_INPUT}" in
    "${USER_INPUT}" )

    local MY_FILE=""

    ;;
esac